/**
 * noop — a no-operation function that accepts any arguments and returns nothing.
 *
 * Useful as a default callback, stub, or placeholder where a function reference
 * is required but no action should be taken.
 */
export function noop(..._args: unknown[]): void {
  // intentionally empty
}
