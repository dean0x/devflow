/**
 * @deprecated Import from './kb/index.js' directly.
 * This module is a compatibility shim preserved for callers that
 * reference the old `commands/kb.js` path (e.g., tests, init.ts).
 */
export * from './kb/index.js';
